Build script will comple the kernel module and the test program.
And it will load the kernel module to your kernel,
run;
1. ./build.sh
2. ./serial

You need to remove kernel module from your kernel:
3.sudo rmmod ttyM.ko
